{
  "name": "storyblok-backup-and-restore",
  "version": "0.1.0",
  "type": "module",
  "description": "CLI and library for backuping and restoring Storyblok resources, supporting both single and bulk operations.",
  "main": "dist/shared/index.js",
  "types": "dist/shared/index.d.ts",
  "bin": {
    "storyblok-backup-and-restore": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/cli/index.ts --format esm --dts --out-dir dist --clean",
    "prepublishOnly": "npm run build",
    "lint": "eslint . --ext .ts",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "storyblok",
    "restore",
    "cli",
    "cms",
    "backup"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.0",
    "glob": "^11.0.3",
    "minimist": "^1.2.8",
    "storyblok-js-client": "^5.0.0",
    "toposort": "^2.0.2"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/node": "^24.0.8",
    "@types/toposort": "^2.0.7",
    "eslint": "^9.3.0",
    "prettier": "^3.6.2",
    "tsc-alias": "^1.8.16",
    "tsup": "^8.5.0",
    "typescript": "^5.0.0"
  }
}
